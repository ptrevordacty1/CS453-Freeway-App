To run this program:
1. Open a terminal and navigate to the source folder in this directory.
2. Start a mongodb database with the command "mongod --dbpath data/db"
3. Open a second terminal in the same directory as above and run the command "node server.js"
4. Open a browser and enter "localhost:3000" in the address bar.